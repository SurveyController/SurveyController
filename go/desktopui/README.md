# SurveyController Desktop UI

Wails v3 桌面壳。

前端使用 Vue 3、TypeScript、TailwindCSS，结构参考 SuiDemo。

## 常用命令

```bash
wails3 generate bindings
wails3 dev
wails3 build
```

前端单独检查：

```bash
cd frontend
npm run check
npm run build
```
